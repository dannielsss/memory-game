import { Container } from '@/styled-components/Container.styled';
import { styled } from 'styled-components';

/**
 * Container for the card game
 */
export const GameCardContainer = styled(Container)`
  background: #ffffff;
  border-radius: 10px;
  padding: 1rem;

  height: auto;
  width: 100%;
  height: 120px;
`;
